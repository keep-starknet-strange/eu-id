#!/usr/bin/env bash
# Run the same Clippy and rustfmt checks as CI.
# Enable this hook with `./scripts/install-hooks.sh`.
# Bypass this hook with `git commit --no-verify`.
exec "$(git rev-parse --show-toplevel)/scripts/check.sh"
