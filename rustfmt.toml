# rustfmt configuration for the eu-id workspace.
#
# This configuration fixes two stable settings.
# All other settings use rustfmt defaults.
# A future style change can add more options.
# Run `make fmt` once after such a change.
edition = "2021"
max_width = 100
