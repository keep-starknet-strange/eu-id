[package]
name = "stwo-p256"
version.workspace = true
edition.workspace = true

[features]
default = ["std"]
std = ["stwo/std", "stwo-constraint-framework/std"]

[dependencies]
air-core = { path = "../air-core" }
stwo = { workspace = true, features = ["parallel"] }
stwo-constraint-framework = { workspace = true, features = ["parallel"] }
stwo-p256-utils = { path = "../stwo-p256-utils" }
rayon = "1"
itertools = { version = "0.12", default-features = false, features = ["use_alloc"] }
num-bigint = "0.4"
num-integer = "0.1"
num-traits = { workspace = true }
rand = "0.8"
serde = { workspace = true }

[dev-dependencies]
p256 = { version = "0.13", features = ["ecdsa"] }
ecdsa = { version = "0.16", features = ["signing", "verifying"] }
sha2 = "0.10"

[lints]
workspace = true
