/build/
/.gradle/
/.kotlin/
# Machine-specific SDK/NDK paths.
local.properties
# Ignore outputs from SDK builds that predate the build-directory variant API.
# The current build excludes these paths and writes only below `/build/`.
/src/main/jniLibs/
/src/main/kotlin/
